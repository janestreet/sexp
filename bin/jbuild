(executables (
  (names (main))
  (public_names (sexp))
  (libraries (async
              core
              sexp_app
              sexp_pretty
              sexplib
              csvfields.xml-light))
  (flags (:standard -g))
  (preprocess (pps (ppx_jane ppxlib.runner)))))


(jbuild_version 1)
